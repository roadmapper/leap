<div class= "container">
<h1>Upload</h1>

<h3>Processed Data</h3>
<%= link_to 'Information to be Uploaded', stagings_path %>

<h3>Upload</h3>
		<%= form_tag({:action => :upload,  :controller=>"uploads"}, :multipart => true) do %>
		  <%= file_field_tag 'file' %>
		<br>
		<%= submit_tag( "UploadElectric", class: 'btn-small btn-primary') %>
		<br>
		<% end %>
		
    <br>
		<%= form_tag({:action => :uploadGas,  :controller=>"uploads"}, :multipart => true) do %>
		  <%= file_field_tag 'file' %>
		<br>
		<%= submit_tag( "UploadGas", class: 'btn-small btn-primary') %>
		<br>
		<% end %>

		<div id="ajax">
		<!--% render :partial => "report" %-->
		</div>


<h3>Current Uploaded Files</h3>

<table class="table table-striped">
  <tr>
    <th>File Name</th>
    <th>Status</th>
    <th>Upload Date</th>
    <th>Process Date</th>
    <th>Uploaded by</th>
    <th></th>
    <th></th>
  </tr>

<% @uploads.each do |upload| %>
  <tr>
    <td><%= upload.file_name %></td>
    <td><%= upload.status %></td>
    <td><%= upload.upload_date %></td>
    <td><%= upload.process_date %></td>
    <td><%= upload.uploaded_by %></td>
    <td><%#link_to 'Process', process_uploads_path(upload, method: :get), data: { confirm: 'Are you sure?' } %></td>
    <td><%= link_to 'Destroy', upload, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
</div>
