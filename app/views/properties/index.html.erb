<h1>Properties</h1>

<table border = 1>
  <tr>
    <th><%= sortable_column "Customer Unique ID" %></th>
    <th><%= sortable_column "Owner name"%></th>
    <th><%= sortable_column "Street address"%></th>
    <th><%= sortable_column "State"%></th>
    <th><%= sortable_column "Zipcode"%></th>
    <th><%= sortable_column "Finish date"%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @properties.each do |property| %>
  <tr>
    <td><%= property.customer_unique_id %></td>
    <td><%= property.owner_name %></td>
    <td><%= property.street_address %></td>
    <td><%= property.state %></td>
    <td><%= property.zipcode %></td>
    <td><%= property.finish_date %></td>
    <td><%= link_to 'Show', property %></td>
    <td><%= link_to 'Edit', edit_property_path(property) %></td>
    <td><%= link_to 'Destroy', property, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
<%= will_paginate @properties %>

</table>

<br />

<%= link_to 'New Property', new_property_path %>
<%= link_to 'Back', root_path %>
