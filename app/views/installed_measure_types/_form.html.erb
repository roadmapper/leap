<%= form_for(@installed_measure_type) do |f| %>
  <% if @installed_measure_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@installed_measure_type.errors.count, "error") %> prohibited this installed_measure_type from being saved:</h2>

      <ul>
      <% @installed_measure_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :installed_measures %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
