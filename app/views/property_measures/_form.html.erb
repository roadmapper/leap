<%= form_for(@property_measure) do |f| %>
  <% if @property_measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_measure.errors.count, "error") %> prohibited this property_measure from being saved:</h2>

      <ul>
      <% @property_measure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :property %><br />
    <%= f.text_field :property %>
  </div>
  <div class="field">
    <%= f.label :measure %><br />
    <%= f.text_field :measure %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
